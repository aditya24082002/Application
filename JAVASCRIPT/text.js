function findGreatestNumber(){
    var a=100;
    var b=10;
    var c=10;
    (a>b && a>c)? document.write(a + " is greatest Number") : (b>a && b>c) ? document.write(b + " is greatest Number") : (c>a && c>b)? document.write(c + " is greatest Number") :document.write(c + " is equal among any one of " + a + " and " + b);
}

function findVote()
{
    document.write("Webdeveloper");
}

